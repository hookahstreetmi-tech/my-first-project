# TECHNICAL_STACK: Инженерный фундамент

## 🛠 Технологии
- **Core**: n8n (Automation)
- **Database**: Supabase (PostgreSQL)
- **AI Models**: GPT-4o, Perplexity (Sonar)
- **Interface**: Telegram API, Gmail API

## 📊 Схема Данных (Supabase)
1.  **Topics**: `id (UUID)`, `title`, `created_at`.
2.  **Knowledge**: `id`, `topic_id (FK)`, `text`, `sources (JSON)`. 
3.  **Hypotheses**: `id`, `topic_id (FK)`, `title`, `description`, `confidence_score`.
4.  **Tasks**: `id`, `hypothesis_id (FK)`, `title`, `status`, `due_date`.

## 🧬 Архитектурные Решения (Best Practices)
- **Atomic Save**: Всегда сохранять данные в `knowledge` ДО запуска `Trigger Brain Center`. Это предотвращает "гонку данных".
- **Topic ID Propagation**: UUID темы пробрасывается через все воркфлоу как единственный источник правды.
- **Immutable Workflows**: ЗАПРЕЩЕНО самостоятельно изменять существующие рабочие воркфлоу. Любые изменения логики требуют обязательного согласования с Пользователем или предоставления ему четких инструкций для ручного внесения правок.
- **Partial Updates**: Если модификация одобрена, использовать только `update_partial`.

## 🛡 Безопасность
- Все ключи хранятся только в n8n Credentials. 
- Прямой доступ к БД только через защищенные ноды с ограниченными правами.

---
*Сэр, техстек описан. Каждое соединение проверено на элегантность.*
