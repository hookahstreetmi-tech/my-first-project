# Инструкции для Агентов (AGENTS.md)

## 🛡 Права доступа для n8n-mcp
Ниже определены строгие правила использования инструментов n8n-mcp. Агент ОБЯЗАН следовать этим разрешениям:

```json
{
  "permissions": {
    "allow": [
      "mcp__n8n-mcp__search*",
      "mcp__n8n-mcp__get*",
      "mcp__n8n-mcp__list*",
      "mcp__n8n-mcp__validate*"
    ],
    "ask": [
      "mcp__n8n-mcp__create*",
      "mcp__n8n-mcp__update*",
      "mcp__n8n-mcp__execute*"
    ],
    "deny": [
      "mcp__n8n-mcp__delete*"
    ]
  }
}
```

## 📝 Пояснения к правам
*   **Разрешено (allow):** Поиск, получение информации, листинг и валидация воркфлоу/узлов выполняются автоматически. ✅
*   **Спрашивать (ask):** Создание, обновление и запуск (execute) воркфлоу требуют явного подтверждения от пользователя. ❓
*   **Запрещено (deny):** Любые операции удаления полностью заблокированы. 🚫
