# CHAT SNAPSHOT — 2026-02-02

## 🎯 Основная цель сессии

Исправление проблем с Auto-Dispatcher (календарь не заполняется деталями заказов) и переход к архитектуре массового продукта.

---

## 📋 Что было сделано

### 1. Диагностика проблемы "пустого описания в календаре"

**Проблема**: ИИ-агенты создавали события в Google Calendar, но поле `description` оставалось пустым или содержало неправильные данные (старые адреса).

**Причины**:

- Несоответствие названий полей между промптами ИИ и параметрами нод Google Calendar в n8n.
- ИИ-агент "галлюцинировал" (утверждал, что создал событие, но не вызывал инструмент).
- Память агента (Window Buffer Memory) "залипала" на старых адресах из истории переписки.

**Решения**:

- Стандартизировали названия полей: `summary`, `description`, `startTime`, `endTime`.
- Обновили параметры нод `Create Event`, `Update Event`, `Get Events` в воркфлоу `CALENDAR 2 AUTO`.
- Уменьшили размер памяти до 3 сообщений и добавили правило приоритета последнего сообщения.

### 2. Проблема "бреда" ИИ и переспрашивания

**Проблема**: Агент либо игнорировал данные из голосового сообщения, либо просил "предоставить сообщение водителя", хотя оно уже было.

**Причины**:

- Промпт был помещен в поле `Prompt` (текст задания) вместо `System Message` (личность агента).
- Whisper допускал ошибки транскрипции ("Денцова" вместо "Одинцово").
- Слишком "разговорный" промпт вместо технического.

**Решения**:

- Разделили `System Message` (правила агента) и `Prompt` (входные данные).
- Предложили использовать "Vocabulary Prompt" в Whisper для исправления частых опечаток.

### 3. Переход к архитектуре массового продукта

**Вопрос**: Потянет ли n8n массовый продукт? Писать ли код?

**Решение**:

- **Lean MVP подход**: Сначала валидация спроса на простой версии (n8n + AI), потом — код.
- **Hybrid Architecture**: n8n как оркестратор (Telegram, Calendar, Whisper), код (Node.js) для сложной логики (геокодер, мультитенантность).
- **Verification Layer**: Добавление кнопок подтверждения в Telegram, чтобы водитель проверял данные перед записью в календарь.

### 4. Обновление архитектуры проекта

Файл `PROJECTS/project_auto_dispatcher.md` полностью переписан:

- Убрана сложная телефония (SIP) из MVP.
- Добавлен слой "Verification Layer" с кнопками подтверждения.
- План разработки MVP за 3 дня.
- Экономика: COGS ~400-500₽/мес, подписка 2500-3500₽/мес, цель — 100 водителей.

### 5. Обсуждение Value Proposition

**Боли водителей**:

- Забытые заказы = потерянные деньги.
- Двойное бронирование.
- Потеря времени на ручную запись.
- Нет истории клиентов.

**Решение**:

- Автоматический календарь (0 забытых заказов).
- Защита от конфликтов.
- Экономия 1 часа в день = 1-2 дополнительных заказа.
- История всех заказов в Google Calendar.

**ROI**: 1 забытый заказ (4000₽) > стоимость подписки (2500₽). Окупаемость с первого месяца.

---

## 🛠 Технические изменения в n8n

### Воркфлоу: MAIN 2 AUTO (el5R22gFowzVSAfQ)

- Обновлен промпт ноды `Авто АССИСТЕНТ`.
- Настроена нода `Calendar Agent` (Workflow Tool) с входным параметром `query`.
- Память `Window Buffer Memory` уменьшена до 3 сообщений (позже отключена).

### Воркфлоу: CALENDAR 2 AUTO (NV855k7t7p1yteY9)

- Обновлены параметры нод:
  - `Create Event`: добавлено поле `description`.
  - `Update Event`: стандартизированы поля.
  - `Get Events`: обновлены фильтры `timeMin`, `timeMax`.
- Обновлен промпт ноды `Calendar agent` для технического выполнения.

---

## 📂 Измененные файлы

1. **`PROJECTS/auto_dispatcher_prompts.md`** — Промпты для AI-агентов (обновлялись несколько раз).
2. **`PROJECTS/project_auto_dispatcher.md`** — Полностью переписана архитектура проекта (Lean MVP).

---

## 🚀 Следующие шаги

### Краткосрочные (MVP за 3 дня)

1. **День 1**: Финализировать промпты для JSON-экстракции без "бреда".
2. **День 2**: Добавить кнопки подтверждения в Telegram (Inline Keyboard).
3. **День 3**: Тестирование с 2-3 водителями.

### Среднесрочные (Scaling)

1. Интеграция геокодера (API Карт) для проверки адресов.
2. SIP-интеграция (Zadarma) для записи звонков клиентов.
3. Supabase для хранения профилей водителей.
4. Web Dashboard для настройки услуг и цен.

---

## 🧠 Ключевые инсайты

1. **n8n хорош для оркестрации, но не для сложной бизнес-логики**. Для массового продукта нужен код (Node.js/Python).
2. **Confirmation Loop — критичен для доверия**. Водитель должен видеть, что бот понял правильно, прежде чем запись попадет в календарь.
3. **Lean MVP > Идеальный продукт**. Сначала валидация спроса на простой версии, потом — масштабирование.
4. **ROI должен быть очевиден**: 1 забытый заказ = окупаемость подписки.

---

## 🔧 Текущее состояние системы

- **Воркфлоу активны**: MAIN 2 AUTO, CALENDAR 2 AUTO.
- **Проблемы**: ИИ иногда "бредит" из-за памяти и нечетких промптов.
- **Статус**: 80% готовности MVP. Осталось добавить кнопки подтверждения.

---

## 💬 Важные договоренности

- **Не делать изменений в n8n без подтверждения пользователя**.
- **Фокус на Lean MVP**, а не на идеальной архитектуре.
- **Валидация спроса перед глубокой разработкой**.

---

_Snapshot создан: 2026-02-02 16:16 MSK_
