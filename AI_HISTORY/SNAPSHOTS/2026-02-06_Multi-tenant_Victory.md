# Snapshot: Multi-tenant Architecture Success (06.02.2026)

## 📌 Итоговое решение: Динамический Calendar ID

После серии тестов и борьбы с ограничениями n8n, мы внедрили стабильную схему проброса ID календарей.

### 🧩 Компоненты решения:

1.  **MAIN 2 AUTO (Диспетчер)**:
    - Извлекает `calendar_id` из Google Sheets через `Lookup Whitelist`.
    - Передает его в ноду `Call 'CALENDAR AUTO 2'` через `Workflow Inputs`.
    - Схема входа в под-воркфлоу была очищена от дублей, что устранило ошибку `null`.

2.  **CALENDAR AUTO 2 (Исполнитель)**:
    - **Системный промпт Агента**: Содержит блок `CRITICAL INSTRUCTION`, где ID календаря подставляется напрямую из триггера: `{{ $node["When Executed by Another Workflow"].json.calendar_id }}`.
    - **Инструменты (Google Calendar Tool)**: Используют выражение `{{ $fromAI('calendar_id', '', 'string') }}`.
    - Это позволяет избежать ошибки "connect node to...", так как инструмент не ссылается на триггер напрямую, а получает данные от ИИ, который в свою очередь берет их из промпта.

### 🛡 Преимущества:

- **Масштабируемость**: Чтобы добавить нового водителя, достаточно добавить строку в Google Таблицу.
- **Изоляция**: Каждый водитель видит только свои события.
- **Стабильность**: Мы обошли баги n8n с физическими связями нод-инструментов.

---

_Сэр, система Multi-tenant официально запущена. Это сердце нашего будущего Micro-SaaS._ 🚀🛡
