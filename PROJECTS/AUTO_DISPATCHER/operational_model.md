# 🛠 OPERATIONAL MODEL: Onboarding, Support, Billing & Reviews

## 1. Карта подключения водителя (Driver Onboarding Map)

Процесс активации пользователя от первого контакта до полной автоматизации.

### 📍 Текущий этап: Гибридное управление (MVP)

1.  **Первичный контакт**: Водитель пишет `/start` -> Получает отказ с указанием своего **Chat ID**.
2.  **Ручная верификация**:
    - Водитель присылает свой ID и Email (Gmail) администратору.
    - Администратор вносит ID и Email в Google Таблицу `Whitelist` (`status: active`).
3.  **Связка календаря (Manual Sync)**:
    - Водитель один раз открывает настройки своего календаря Google.
    - Добавляет Email сервисного аккаунта бота (см. `credentials`) с правами на запись.
4.  **Активация**: Бот отправляет приветственное сообщение с подтверждением готовности.

### 📍 Целевой этап: Полная автоматизация (Scale)

1.  **Вход**: Регистрация через Telegram WebApp.
2.  **Биллинг**:
    - Выбор тарифа -> Оплата (ЮMoney/Stripe).
    - Webhook мгновенно активирует статус `active` в базе (Supabase).
3.  **One-Click OAuth**:
    - Кнопка "Привязать Календарь".
    - Водитель логинится в Google через OAuth2 интерфейс бота.
    - Система сама получает все права и сохраняет `refresh_token`.
4.  **Zero-Touch Go-Live**: Система готова к работе без участия администратора.

## 2. Система поддержки (AI-Driven Support)

- **Layer 1 (Automation)**: Встроенный RAG-чат на базе документации проекта. Отвечает на вопросы по настройке и командам.
- **Layer 2 (Human)**: Кнопка "Связаться с человеком". Создает тикет в административный чат владельца.
- **Feedback loop**: Анализ частых вопросов для улучшения промптов системы.

## 3. Тарифы и Монетизация

- **FREE (Trial)**: 3 дня полного доступа или 5 заказов.
- **BASE (1490 руб/мес)**: Полный функционал парсинга и записи.
- **TURBO (2990 руб/мес)**: Функции роста заказов ("Соседский эффект", "Отзыв-бустер"), приоритетный саппорт.

## 4. Механика оплаты (ЮMoney Integration)

1.  **Trigger**: Кнопка "Оплатить" в WebApp или уведомление об окончании триала.
2.  **Payment Process**: Генерация ссылки/формы ЮMoney через HTTP-узел в n8n.
3.  **Confirmation (Webhook)**: n8n получает HTTP POST от ЮMoney -> Проверяет сумму и `label` (chat_id) -> Обновляет `status` и `expiry_date` в Supabase.
4.  **Success Notify**: Бот отправляет уведомление об успешном продлении.

## 5. Петля отзывов (Reputation Management)

- **Для клиента**: Сбор оценок после выполнения заказа (автоматический запрос через ТГ/SMS).
- **Для продукта**: Регулярный опрос водителей о качестве распознавания и сэкономленном времени.
- **Goal**: Формирование "Стены доверия" для привлечения новых пользователей.

---

_Документ зафиксирован 04.02.2026. Операционная модель утверждена Сэром._
