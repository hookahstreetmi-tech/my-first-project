# План «Конвейер 1.0» (Интерактив + Автопилот)

Этот план описывает превращение разрозненных воркфлоу в единую автономную систему, управляемую через Telegram.

## 1. Входные ворота (Inlet Gateway) - [ВЫПОЛНЕНО]
Реализован механизм приема ручных и автоматических тем:
- **Topic Generator**: Ежедневный запуск (08:00) для фоновых исследований.
- **Manual Telegram Input**: Прямой ввод в чат (обработка через GPT-4o-mini).
- **VIP Lane**: Ручные темы получают флаг `deep_search` (если запрошено) и запускают `Research Team` мгновенно.

## 2. Интерактивное управление (Approval Loop) - [ВЫПОЛНЕНО]
Управление производством контента через Inline-кнопки.

### Stage 1: Brain Center -> Telegram - [ВЫПОЛНЕНО]
При отправке гипотезы в Telegram нода содержит кнопки:
- `[ 🚀 В работу ]` (callback_data: `prod|{{id}}`)
- `[ 🗑️ Архив ]` (callback_data: `arch|{{id}}`)

### Stage 2: Callback Dispatcher - [ВЫПОЛНЕНО]
Новый воркфлоу `5) Dispatcher (Indus.8M)` (ID: `mBzsfg2gW1iOEQeJ`):
- Слушает `callback_query`.
- **Логика `prod`**:
  1. Обновляет статус в `hypotheses` на `validated`.
  2. Запускает `Production Agent`.
  3. Уведомляет пользователя через всплывающее окно Telegram.

## 3. Обновление Базы Данных - [ВЫПОЛНЕНО]
Таблица `topics` расширена:
- `deep_search` (BOOLEAN) — для переключения режимов парсинга.
- `source_workflow` (TEXT) — для аналитики (auto/manual).
- `hypotheses` — автоматическое обновление статуса при нажатии кнопок.

## Таблица автоматизации (Матрица Конвейера)

| Этап | Имя Агента | n8n ID | Статус | Автоматизация |
| :--- | :--- | :--- | :--- | :--- |
| 1 | Topic Generator | `QTYaCNJB6hChBlIA` | ✅ Active | Schedule / Manual |
| 2 | Research Team | `ItU0fZMVllwFxFDQ` | ✅ Active | Вызывается из (1) |
| 3 | Brain Center | `ggcoBC7nujBUXwmB` | ✅ Active | Вызывается из (2) |
| 4 | Dispatcher | `mBzsfg2gW1iOEQeJ` | ✅ Active | Telegram Callback |
| 5 | Production | `n14qDgi3nFiGn9Fm` | ✅ Active | Вызывается из (4) |

---
*Статус системы: Полная автономия достигнута. Все связи верифицированы 28.01.2026.*
