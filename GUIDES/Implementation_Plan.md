# План "Автопилот + Интерактив" для Indus.8M

Этот план описывает превращение разрозненных воркфлоу в единую автономную систему, управляемую через Telegram.

## 1. Входные ворота (Inlet Gateway)
Реализован механизм приема ручных и автоматических тем:
- **Topic Generator**: Ежедневный запуск (08:00) для фоновых исследований.
- **Manual Telegram Input**: Прямой ввод в чат (обработка через GPT-4o-mini).
- **VIP Lane**: Ручные темы получают флаг `deep_search` (если запрошено) и запускают `Research Team` мгновенно.

## 2. Интерактивное управление (Approval Loop)
Управление производством контента через Inline-кнопки.

### Stage 1: Brain Center -> Telegram
При отправке гипотезы в Telegram нода содержит кнопки:
- `[ 🚀 В работу ]` (callback_data: `prod_{{id}}`)
- `[ 🗑️ В архив ]` (callback_data: `arch_{{id}}`)
- При нажатии бот меняет сообщение на: "⏳ Обрабатываю..." (Edit Message).

### Stage 2: Callback Dispatcher
Новый воркфлоу (или Telegram Trigger в Production):
- Слушает `callback_query`.
- **Логика `prod_*`**:
  1. Обновляет статус в `hypotheses` на `validated`.
  2. Запускает `Production Agent`.
  3. Уведомляет пользователя о начале генерации контента.

## 3. Обновление Базы Данных
Таблица `topics` расширена:
- `deep_search` (BOOLEAN) — для переключения режимов парсинга.
- `source_workflow` (TEXT) — для аналитики (auto/manual).

## Верификация
1. Тест ручного ввода: "Исследуй [тема] подробно".
2. Проверка появления `deep_search: true` в базе.
3. Получение гипотезы с кнопками.
4. Нажатие кнопки и автоматический пост.
