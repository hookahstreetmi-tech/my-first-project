{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "bashdb",
            "request": "launch",
            "name": "🚀 RUN: execute_me.sh",
            "program": "${workspaceFolder}/TOOLS/execute_me.sh"
        }
    ]
}